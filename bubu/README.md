# BuBu Courier App (Expo + React Native)

Minimal skeleton generated by AI. Run the following to start in development:

```bash
cd bubu
npm install # or yarn
npm start    # opens Expo DevTools
```

Use `Dev Login` button to jump directly into customer flow.

This is only a placeholder implementation; see project roadmap in original prompt for full tasks.